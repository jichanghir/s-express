const mongoose = require('mongoose');
const Skill = require('../models/skill');

module.exports = () => {
    return new Promise((resolve, reject) => {
        Skills.find({})
        .then((result) => {

        })
    });
}
